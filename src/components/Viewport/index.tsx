import { EmailContent } from 'components/EmailContent'
import { ViewportContainer } from 'components/Viewport/styled'

export default function Viewport() {
  return (
    <ViewportContainer>
      <EmailContent />
    </ViewportContainer>
  )
}
